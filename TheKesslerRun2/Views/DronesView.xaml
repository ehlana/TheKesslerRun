<UserControl x:Class="TheKesslerRun2.Views.DronesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:TheKesslerRun2.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:DronesViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,8,8" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Header="Fleet Status" Grid.Row="0" Grid.Column="0">
            <ListView ItemsSource="{Binding Drones}"
                      SelectedItem="{Binding SelectedDrone, Mode=TwoWay}"
                      Margin="0">
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn Header="Drone" DisplayMemberBinding="{Binding DisplayName}" Width="100" />
                        <GridViewColumn Header="State" DisplayMemberBinding="{Binding State}" Width="110" />
                        <GridViewColumn Header="Destination" DisplayMemberBinding="{Binding DestinationLabel}" Width="170" />
                        <GridViewColumn Header="Charge" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <ProgressBar Width="90" Height="14" Value="{Binding ChargePercent}" Maximum="1" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding ChargePercent, StringFormat={}{0:P0}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Cargo" Width="160">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <ProgressBar Width="90" Height="14" Value="{Binding CargoPercent}" Maximum="1" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding CurrentCargo, StringFormat={}{0:0}}" Margin="0,0,6,0" />
                                        <TextBlock Text="/" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding MaxCargo, StringFormat={}{0:0}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Cargo Manifest" DisplayMemberBinding="{Binding CargoType}" Width="160" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <GroupBox Header="Known Fields" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="Select a target field to dispatch a drone." />
                <ListBox ItemsSource="{Binding KnownFields}"
                         SelectedItem="{Binding SelectedField, Mode=TwoWay}"
                         Margin="0,8,0,0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding ResourceType}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding OutputSummary}" TextWrapping="Wrap" />
                                    <TextBlock Text="{Binding DistanceFromCentre, StringFormat={}{0:0} km away}" />
                                    <TextBlock Text="{Binding ResourceAmount, StringFormat={}{0:0} units}" />
                                <TextBlock Text="{Binding MiningDifficulty, StringFormat=Difficulty {0:0.0}}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </GroupBox>

        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Padding="12" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding DronesLost, StringFormat=Total drones lost: {0}}" />
                </StackPanel>
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0"
                            VerticalAlignment="Center">
                    <Button Content="Recall Drone"
                            Padding="16,6"
                            Margin="0,0,8,0"
                            Command="{Binding RecallDroneCommand}" />
                    <Button Content="Launch Drone"
                            Padding="20,6"
                            Command="{Binding SendDroneCommand}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
